<p id="notice"><%= notice %></p>

<h1>Pronouns</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Nominative</th>
      <th>Accousative</th>
      <th>Dative</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pronouns.each do |pronoun| %>
      <tr>
        <td><%= pronoun.description %></td>
        <td><%= pronoun.nominative %></td>
        <td><%= pronoun.accusative%></td>
        <td><%= pronoun.dative %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Home', root_path %>
